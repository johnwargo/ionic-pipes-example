import { CommaObjectPipe } from './comma-object.pipe';

describe('CommaObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
